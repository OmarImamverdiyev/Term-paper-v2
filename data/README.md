Place source CSV datasets here when you want a clean `data/` layout.

The pipeline also accepts CSV paths outside this folder, so the bundled sample config points to the existing root-level Sentiment140 sample in this workspace.
